# Django 5.2.8+ supports Python 3.14 (the project virtualenv uses 3.14).
Django>=5.2.8,<5.3
djangorestframework
djangorestframework-simplejwt
django-filter
psycopg2-binary
Pillow
requests
python-decouple
django-cors-headers
celery
redis

# ── AI Agent dependencies ──────────────────────────────────
sentence-transformers          # semantic search embeddings
scikit-learn                   # intent classification
joblib                         # sklearn model serialisation
numpy                          # numerical ops for embeddings
opencv-python-headless         # medicine image feature extraction
langdetect                     # Roman Urdu vs English detection
